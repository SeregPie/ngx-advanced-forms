import {FormControl} from '@angular/forms';
import {noop} from 'rxjs';

import {BetterFormArray} from './better-form-array';

describe('BetterFormArray', () => {
	describe('push', () => {
		it('should work', () => {
			// todo: test
		});

		it('should trigger changes only once', () => {
			// todo: test
		});
	});

	describe('pushAll', () => {
		it('should work', () => {
			// todo: test
		});

		it('should trigger changes only once', () => {
			// todo: test
		});
	});

	describe('addAll', () => {
		it('should work', () => {
			// todo: test
		});

		it('should trigger changes only once', () => {
			// todo: test
		});
	});

	describe('set', () => {
		it('should work', () => {
			// todo: test
		});

		it('should trigger changes only once', () => {
			// todo: test
		});
	});

	describe('remove', () => {
		it('should work', () => {
			// todo: test
		});

		it('should trigger changes only once', () => {
			// todo: test
		});
	});

	describe('empty', () => {
		it('should work', () => {
			// todo: test
		});
	});

	describe('size', () => {
		it('should work', () => {
			// todo: test
		});
	});
});
